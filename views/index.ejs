<head>
    <% include templates/head.ejs%>
</head>
<% include templates/header.ejs%>

    <div class="container">
        <h2>Blog</h2>
        <hr>
        <% posts.forEach(function(post) { %>
            <a href="/blog/<%= post.slug %>">
                 <h2 class="ml-2" style="font-family:'kanit', sans-serif;color:#292929;text-decoration:none;margin-bottom:15px"><%= post.title %></h2>
                 <div class="container">
                    <div class="row">
                      <div class="col-md-12 col-sm-12">
                        <img style="min-height:45%;width:80%;margin-bottom:15px;margin-left:0%" src="<%= post.featured_image %>">
                      </div>
                      <div class="col-md-12 col-sm-12">
                         <p style="color:gray;text-decoration:none;font-size:1.25em;font-family:'kanit', sans-serif;"><%= post.summary %> <br><br> 
                         Author : <%= post.author.first_name %> <%= post.author.last_name %>
                        </p>
                      </div>
                 </div>  <hr>   

            </a>    <br>
                    <% }); %>

                        <% if (previous_page) { %>
                            <a href="/blog/p/<%= previous_page %>">Prev</a>
                            <% } %>

                                <% if (next_page) { %>
                                    <a href="/blog/p/<%= next_page %>">Next</a>
                                    <% } %>
                                        <div>
                                        <br>
                                           <div class="float=right"><% include templates/footer.ejs%></div>